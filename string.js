const nama = "januarsyah akbar";
console.info(nama.length);
console.info(nama.toLowerCase());
console.info(nama.toUpperCase());
console.info(nama.split(" ")); // it will cahange to array with split the spasi

const input = "    my name is akbar    ";
console.info(input.trim()); // it will remove the spasi in the start and end
